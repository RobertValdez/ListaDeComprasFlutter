import 'package:flutter/material.dart';

class MaintProductEdit extends StatelessWidget {
  const MaintProductEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('MaintProductEdit')),
      body: const Center(child: Text('Esta es la nueva pantalla')),
    );
  }
}
