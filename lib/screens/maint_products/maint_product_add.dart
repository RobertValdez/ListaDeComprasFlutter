import 'package:flutter/material.dart';

class MaintProductAdd extends StatelessWidget {
  const MaintProductAdd({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('MaintProductAdd')),
      body: const Center(child: Text('Esta es la nueva pantalla')),
    );
  }
}
